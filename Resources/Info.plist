<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Template. build.sh substitutes __VERSION__, __FEED_URL__ and __ED_KEY__ from
  VERSION and release.conf, so the version and the update feed are stated once.
-->
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>Claude Live</string>
	<key>CFBundleDisplayName</key>
	<string>Claude Live</string>
	<key>CFBundleExecutable</key>
	<string>ClaudeLive</string>
	<key>CFBundleIdentifier</key>
	<string>it.aldeialab.ClaudeLive</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>__VERSION__</string>
	<key>CFBundleVersion</key>
	<string>__VERSION__</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>LSMinimumSystemVersion</key>
	<string>14.0</string>
	<!-- Il nome mostrato all'utente. Non ha relazione con CFBundleIdentifier
	     (`it.aldeialab.ClaudeLive`), che resta com'è: cambiarlo romperebbe il
	     feed Sparkle delle copie installate, la ACL della voce Keychain, la
	     registrazione SMAppService e il dominio UserDefaults. Nemmeno con
	     l'intestatario del certificato di firma, che è per forza la
	     denominazione legale dell'account Apple. -->
	<key>NSHumanReadableCopyright</key>
	<string>Purple Heads</string>

	<!-- Menu bar only: no Dock icon, no app switcher entry. -->
	<key>LSUIElement</key>
	<true/>

	<!-- No Accessibility or Automation usage descriptions on purpose: the
	     project list comes from VS Code's own `code --status` CLI, so the app
	     needs no TCC permission at all. -->

	<!-- Sparkle: automatic updates. -->
	<key>SUFeedURL</key>
	<string>__FEED_URL__</string>
	<key>SUPublicEDKey</key>
	<string>__ED_KEY__</string>
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<key>SUScheduledCheckInterval</key>
	<integer>86400</integer>
	<!-- Updates are fetched over HTTPS from GitHub; no insecure loads needed. -->
	<key>SUAllowsAutomaticUpdates</key>
	<true/>
</dict>
</plist>
